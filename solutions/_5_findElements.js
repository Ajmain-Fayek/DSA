/**
 * You have arrays `A = [1, 2, 3]` and `B = [2, 3, 4]`.
 * Without using built-in methods, explain how you'd find elements in A but not in B.
 *
 * Input: A = [1, 2, 3], B = [2, 3, 4]
 * Output: [1]
 * Goal: To practice comparing two arrays in JavaScript and identifying unique elements
 * using loops and conditional logic without relying on built-in methods.
 */

